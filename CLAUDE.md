# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## プロジェクト概要

Google Apps Script（GAS）で作る「個人・売上・利益率計算表」。案件ごとの売上・原価・利益率、
担当者ごとの稼働率・負荷を可視化し、「誰が・何を・どれだけ担当し・どんな成果を出したか」を
後から説明できるようにするための計算表。詳細は [README.md](README.md) を参照。

- 技術スタック: Google Apps Script（V8ランタイム）、Google スプレッドシート
- リポジトリはコードのみを管理し、clasp によるデプロイ連携は行わない
  （`gas/` 配下の .gs ファイルを Apps Script エディタに手動でコピーして使う）
- 主なファイル: `gas/Config.gs`（設定）, `gas/Main.gs`（メニュー/エントリーポイント）,
  `gas/SetupSheets.gs`（シート初期作成）, `gas/Aggregate.gs`（集計ロジック）

## Git運用ルール

- **コードを変更するたびに、必ず変更内容をコミットし、GitHubへプッシュすること。**
  - 作業を放置してローカルにのみ変更を溜め込まない。1つの意味のある変更単位（1機能・1修正など）ごとにコミット・プッシュを行う。
  - コミットメッセージは変更内容が分かるように簡潔に記述する。
  - プッシュ前に `git status` / `git diff` で差分を確認し、意図しないファイルや機密情報（.env、認証情報など）が含まれていないことを確認する。
  - force push（強制プッシュ）は明示的な指示がない限り行わない。
  - コミットのたびに毎回ユーザーへ確認を取る必要はないが、pushの実行結果は簡潔に報告する。

## 開発コマンド

ビルド・テストは無し（純粋な GAS コード）。動作確認は Google スプレッドシート上で
Apps Script を実行して行う（[README.md](README.md) のセットアップ手順を参照）。
